import { GenericException } from '@app/exceptions';
import { DefaultExtractorResult, {{ProviderName}}Output } from '@app/contracts';
import { ProviderType } from '@app/shared';
import { DefaultParser } from './DefaultParser';

export class {{ProviderName}}Parser extends DefaultParser {

	public override transform(html: string, sourceUrl: string): Partial<DefaultExtractorResult<Partial<{{ProviderName}}Output>>> {
		try {
			return {
				sourceUrl,
				};
		} catch (error) {
			throw new GenericException('Unable to parse some fields:', ProviderType.{{ProviderName}}, '{{ProviderName}}Parser', { cause: error });
		}
	}
}