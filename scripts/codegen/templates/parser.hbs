import { GenericException } from '@app/exceptions';
import { DefaultExecutionResult, {{ProviderName}}Output } from '@app/contracts';
import { ProviderType } from '@app/shared';
import { BaseParser } from '@app/base';

export class {{ProviderName}}Parser extends BaseParser {

	public override transform(html: string, sourceUrl: string): Partial<DefaultExecutionResult<Partial<{{ProviderName}}Output>>> {
		try {
			return {
				customFields: {} as {{ProviderName}}Output
				};
		} catch (error) {
			throw new GenericException('Unable to parse some fields:', ProviderType.{{ProviderName}}, '{{ProviderName}}Parser', { cause: error });
		}
	}
}
