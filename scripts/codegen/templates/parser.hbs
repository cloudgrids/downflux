import { GenericException } from '../exceptions';
import { DefaultExtractorResult, {{ProviderName}}Output } from '@app/shared';
import { DefaultParser } from './DefaultParser';

export class {{ProviderName}}ParserRegistry extends DefaultParser {

	public override transform(html: string, sourceUrl: string): Partial<DefaultExtractorResult<Partial<{{ProviderName}}Output>>> {
		try {
			return {
				description,
				sourceUrl,
				};
		} catch (error) {
			throw new GenericException('Unable to parse some fields:', ProviderType.{{ProviderName}}, '{{ProviderName}}ParserRegistry', { cause: error });
		}
	}
}