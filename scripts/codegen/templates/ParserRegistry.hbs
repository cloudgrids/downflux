import { Provider } from '@types';
import { BaseParser } from '@base';
{{imports}}

export class ParserRegistry {
	private static readonly parsers: Record<Provider, BaseParser> = {
		{{entries}}
	};

	public static getParser(Provider: Provider): BaseParser {
		return this.parsers[Provider] ?? this.parsers[Provider.Default];
	}
}
